/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controller.dao.jdbc;

import co.edu.sena.controller.dao.*;
import co.edu.sena.controller.factory.*;
import co.edu.sena.model.dao.dto.*;
import co.edu.sena.controller.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class AprendizDaoImpl extends AbstractDAO implements AprendizDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT tipo_documento, numero_documento, estado, ficha, grupo_codigo FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( tipo_documento, numero_documento, estado, ficha, grupo_codigo ) VALUES ( ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET tipo_documento = ?, numero_documento = ?, estado = ?, ficha = ?, grupo_codigo = ? WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ? AND grupo_codigo = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ? AND grupo_codigo = ?";

	/** 
	 * Index of column tipo_documento
	 */
	protected static final int COLUMN_TIPO_DOCUMENTO = 1;

	/** 
	 * Index of column numero_documento
	 */
	protected static final int COLUMN_NUMERO_DOCUMENTO = 2;

	/** 
	 * Index of column estado
	 */
	protected static final int COLUMN_ESTADO = 3;

	/** 
	 * Index of column ficha
	 */
	protected static final int COLUMN_FICHA = 4;

	/** 
	 * Index of column grupo_codigo
	 */
	protected static final int COLUMN_GRUPO_CODIGO = 5;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 5;

	/** 
	 * Index of primary-key column tipo_documento
	 */
	protected static final int PK_COLUMN_TIPO_DOCUMENTO = 1;

	/** 
	 * Index of primary-key column numero_documento
	 */
	protected static final int PK_COLUMN_NUMERO_DOCUMENTO = 2;

	/** 
	 * Index of primary-key column ficha
	 */
	protected static final int PK_COLUMN_FICHA = 3;

	/** 
	 * Index of primary-key column grupo_codigo
	 */
	protected static final int PK_COLUMN_GRUPO_CODIGO = 4;

	/** 
	 * Inserts a new row in the aprendiz table.
	 */
	public AprendizPk insert(Aprendiz dto) throws AprendizDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getTipoDocumento() );
			stmt.setString( index++, dto.getNumeroDocumento() );
			stmt.setString( index++, dto.getEstado() );
			stmt.setString( index++, dto.getFicha() );
			stmt.setInt( index++, dto.getGrupoCodigo() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AprendizDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the aprendiz table.
	 */
	public void update(AprendizPk pk, Aprendiz dto) throws AprendizDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getTipoDocumento() );
			stmt.setString( index++, dto.getNumeroDocumento() );
			stmt.setString( index++, dto.getEstado() );
			stmt.setString( index++, dto.getFicha() );
			stmt.setInt( index++, dto.getGrupoCodigo() );
			stmt.setString( 6, pk.getTipoDocumento() );
			stmt.setString( 7, pk.getNumeroDocumento() );
			stmt.setString( 8, pk.getFicha() );
			stmt.setInt( 9, pk.getGrupoCodigo() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AprendizDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the aprendiz table.
	 */
	public void delete(AprendizPk pk) throws AprendizDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getTipoDocumento() );
			stmt.setString( 2, pk.getNumeroDocumento() );
			stmt.setString( 3, pk.getFicha() );
			stmt.setInt( 4, pk.getGrupoCodigo() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AprendizDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the aprendiz table that matches the specified primary-key value.
	 */
	public Aprendiz findByPrimaryKey(AprendizPk pk) throws AprendizDaoException
	{
		return findByPrimaryKey( pk.getTipoDocumento(), pk.getNumeroDocumento(), pk.getFicha(), pk.getGrupoCodigo() );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'tipo_documento = :tipoDocumento AND numero_documento = :numeroDocumento AND ficha = :ficha AND grupo_codigo = :grupoCodigo'.
	 */
	public Aprendiz findByPrimaryKey(String tipoDocumento, String numeroDocumento, String ficha, int grupoCodigo) throws AprendizDaoException
	{
		Aprendiz ret[] = findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ? AND grupo_codigo = ?", new Object[] { tipoDocumento, numeroDocumento, ficha,  new Integer(grupoCodigo) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria ''.
	 */
	public Aprendiz[] findAll() throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY tipo_documento, numero_documento, ficha, grupo_codigo", null );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'estado = :estado'.
	 */
	public Aprendiz[] findByEstadoFormacion(String estado) throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE estado = ?", new Object[] { estado } );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'ficha = :ficha AND grupo_codigo = :grupoCodigo'.
	 */
	public Aprendiz[] findByGrupoProyecto(String ficha, int grupoCodigo) throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ficha = ? AND grupo_codigo = ?", new Object[] { ficha,  new Integer(grupoCodigo) } );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'tipo_documento = :tipoDocumento AND numero_documento = :numeroDocumento'.
	 */
	public Aprendiz[] findByCliente(String tipoDocumento, String numeroDocumento) throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? AND numero_documento = ?", new Object[] { tipoDocumento, numeroDocumento } );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'tipo_documento = :tipoDocumento'.
	 */
	public Aprendiz[] findWhereTipoDocumentoEquals(String tipoDocumento) throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? ORDER BY tipo_documento", new Object[] { tipoDocumento } );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'numero_documento = :numeroDocumento'.
	 */
	public Aprendiz[] findWhereNumeroDocumentoEquals(String numeroDocumento) throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE numero_documento = ? ORDER BY numero_documento", new Object[] { numeroDocumento } );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'estado = :estado'.
	 */
	public Aprendiz[] findWhereEstadoEquals(String estado) throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE estado = ? ORDER BY estado", new Object[] { estado } );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'ficha = :ficha'.
	 */
	public Aprendiz[] findWhereFichaEquals(String ficha) throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ficha = ? ORDER BY ficha", new Object[] { ficha } );
	}

	/** 
	 * Returns all rows from the aprendiz table that match the criteria 'grupo_codigo = :grupoCodigo'.
	 */
	public Aprendiz[] findWhereGrupoCodigoEquals(int grupoCodigo) throws AprendizDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE grupo_codigo = ? ORDER BY grupo_codigo", new Object[] {  new Integer(grupoCodigo) } );
	}

	/**
	 * Method 'AprendizDaoImpl'
	 * 
	 */
	public AprendizDaoImpl()
	{
	}

	/**
	 * Method 'AprendizDaoImpl'
	 * 
	 * @param userConn
	 */
	public AprendizDaoImpl(final Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "observador_de_proyectos.aprendiz";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Aprendiz fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Aprendiz dto = new Aprendiz();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Aprendiz[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Aprendiz dto = new Aprendiz();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Aprendiz ret[] = new Aprendiz[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Aprendiz dto, ResultSet rs) throws SQLException
	{
		dto.setTipoDocumento( rs.getString( COLUMN_TIPO_DOCUMENTO ) );
		dto.setNumeroDocumento( rs.getString( COLUMN_NUMERO_DOCUMENTO ) );
		dto.setEstado( rs.getString( COLUMN_ESTADO ) );
		dto.setFicha( rs.getString( COLUMN_FICHA ) );
		dto.setGrupoCodigo( rs.getInt( COLUMN_GRUPO_CODIGO ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Aprendiz dto)
	{
	}

	/** 
	 * Returns all rows from the aprendiz table that match the specified arbitrary SQL statement
	 */
	public Aprendiz[] findByDynamicSelect(String sql, Object[] sqlParams) throws AprendizDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AprendizDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the aprendiz table that match the specified arbitrary SQL statement
	 */
	public Aprendiz[] findByDynamicWhere(String sql, Object[] sqlParams) throws AprendizDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new AprendizDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
