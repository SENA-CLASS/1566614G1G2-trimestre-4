/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controller.dao.jdbc;

import co.edu.sena.controller.dao.*;
import co.edu.sena.controller.factory.*;
import co.edu.sena.model.dao.dto.*;
import co.edu.sena.controller.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class InstructorFichaDaoImpl extends AbstractDAO implements InstructorFichaDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT tipo_documento, numero_documento, ficha FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( tipo_documento, numero_documento, ficha ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET tipo_documento = ?, numero_documento = ?, ficha = ? WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ?";

	/** 
	 * Index of column tipo_documento
	 */
	protected static final int COLUMN_TIPO_DOCUMENTO = 1;

	/** 
	 * Index of column numero_documento
	 */
	protected static final int COLUMN_NUMERO_DOCUMENTO = 2;

	/** 
	 * Index of column ficha
	 */
	protected static final int COLUMN_FICHA = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column tipo_documento
	 */
	protected static final int PK_COLUMN_TIPO_DOCUMENTO = 1;

	/** 
	 * Index of primary-key column numero_documento
	 */
	protected static final int PK_COLUMN_NUMERO_DOCUMENTO = 2;

	/** 
	 * Index of primary-key column ficha
	 */
	protected static final int PK_COLUMN_FICHA = 3;

	/** 
	 * Inserts a new row in the instructor_ficha table.
	 */
	public InstructorFichaPk insert(InstructorFicha dto) throws InstructorFichaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getTipoDocumento() );
			stmt.setString( index++, dto.getNumeroDocumento() );
			stmt.setString( index++, dto.getFicha() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorFichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the instructor_ficha table.
	 */
	public void update(InstructorFichaPk pk, InstructorFicha dto) throws InstructorFichaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getTipoDocumento() );
			stmt.setString( index++, dto.getNumeroDocumento() );
			stmt.setString( index++, dto.getFicha() );
			stmt.setString( 4, pk.getTipoDocumento() );
			stmt.setString( 5, pk.getNumeroDocumento() );
			stmt.setString( 6, pk.getFicha() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorFichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the instructor_ficha table.
	 */
	public void delete(InstructorFichaPk pk) throws InstructorFichaDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getTipoDocumento() );
			stmt.setString( 2, pk.getNumeroDocumento() );
			stmt.setString( 3, pk.getFicha() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorFichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the instructor_ficha table that matches the specified primary-key value.
	 */
	public InstructorFicha findByPrimaryKey(InstructorFichaPk pk) throws InstructorFichaDaoException
	{
		return findByPrimaryKey( pk.getTipoDocumento(), pk.getNumeroDocumento(), pk.getFicha() );
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the criteria 'tipo_documento = :tipoDocumento AND numero_documento = :numeroDocumento AND ficha = :ficha'.
	 */
	public InstructorFicha findByPrimaryKey(String tipoDocumento, String numeroDocumento, String ficha) throws InstructorFichaDaoException
	{
		InstructorFicha ret[] = findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ?", new Object[] { tipoDocumento, numeroDocumento, ficha } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the criteria ''.
	 */
	public InstructorFicha[] findAll() throws InstructorFichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY tipo_documento, numero_documento, ficha", null );
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the criteria 'tipo_documento = :tipoDocumento AND numero_documento = :numeroDocumento'.
	 */
	public InstructorFicha[] findByCliente(String tipoDocumento, String numeroDocumento) throws InstructorFichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? AND numero_documento = ?", new Object[] { tipoDocumento, numeroDocumento } );
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the criteria 'ficha = :ficha'.
	 */
	public InstructorFicha[] findByFicha(String ficha) throws InstructorFichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ficha = ?", new Object[] { ficha } );
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the criteria 'tipo_documento = :tipoDocumento'.
	 */
	public InstructorFicha[] findWhereTipoDocumentoEquals(String tipoDocumento) throws InstructorFichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? ORDER BY tipo_documento", new Object[] { tipoDocumento } );
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the criteria 'numero_documento = :numeroDocumento'.
	 */
	public InstructorFicha[] findWhereNumeroDocumentoEquals(String numeroDocumento) throws InstructorFichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE numero_documento = ? ORDER BY numero_documento", new Object[] { numeroDocumento } );
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the criteria 'ficha = :ficha'.
	 */
	public InstructorFicha[] findWhereFichaEquals(String ficha) throws InstructorFichaDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ficha = ? ORDER BY ficha", new Object[] { ficha } );
	}

	/**
	 * Method 'InstructorFichaDaoImpl'
	 * 
	 */
	public InstructorFichaDaoImpl()
	{
	}

	/**
	 * Method 'InstructorFichaDaoImpl'
	 * 
	 * @param userConn
	 */
	public InstructorFichaDaoImpl(final Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "observador_de_proyectos.instructor_ficha";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected InstructorFicha fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			InstructorFicha dto = new InstructorFicha();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected InstructorFicha[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			InstructorFicha dto = new InstructorFicha();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		InstructorFicha ret[] = new InstructorFicha[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(InstructorFicha dto, ResultSet rs) throws SQLException
	{
		dto.setTipoDocumento( rs.getString( COLUMN_TIPO_DOCUMENTO ) );
		dto.setNumeroDocumento( rs.getString( COLUMN_NUMERO_DOCUMENTO ) );
		dto.setFicha( rs.getString( COLUMN_FICHA ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(InstructorFicha dto)
	{
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the specified arbitrary SQL statement
	 */
	public InstructorFicha[] findByDynamicSelect(String sql, Object[] sqlParams) throws InstructorFichaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorFichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the instructor_ficha table that match the specified arbitrary SQL statement
	 */
	public InstructorFicha[] findByDynamicWhere(String sql, Object[] sqlParams) throws InstructorFichaDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorFichaDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
