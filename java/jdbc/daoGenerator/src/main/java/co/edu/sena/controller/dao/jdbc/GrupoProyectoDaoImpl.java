/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controller.dao.jdbc;

import co.edu.sena.controller.dao.*;
import co.edu.sena.controller.factory.*;
import co.edu.sena.model.dao.dto.*;
import co.edu.sena.controller.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class GrupoProyectoDaoImpl extends AbstractDAO implements GrupoProyectoDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT numero_ficha, codigo, nombre_proyecto, estado_grupo FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( numero_ficha, codigo, nombre_proyecto, estado_grupo ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET numero_ficha = ?, codigo = ?, nombre_proyecto = ?, estado_grupo = ? WHERE numero_ficha = ? AND codigo = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE numero_ficha = ? AND codigo = ?";

	/** 
	 * Index of column numero_ficha
	 */
	protected static final int COLUMN_NUMERO_FICHA = 1;

	/** 
	 * Index of column codigo
	 */
	protected static final int COLUMN_CODIGO = 2;

	/** 
	 * Index of column nombre_proyecto
	 */
	protected static final int COLUMN_NOMBRE_PROYECTO = 3;

	/** 
	 * Index of column estado_grupo
	 */
	protected static final int COLUMN_ESTADO_GRUPO = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column numero_ficha
	 */
	protected static final int PK_COLUMN_NUMERO_FICHA = 1;

	/** 
	 * Index of primary-key column codigo
	 */
	protected static final int PK_COLUMN_CODIGO = 2;

	/** 
	 * Inserts a new row in the grupo_proyecto table.
	 */
	public GrupoProyectoPk insert(GrupoProyecto dto) throws GrupoProyectoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getNumeroFicha() );
			stmt.setInt( index++, dto.getCodigo() );
			stmt.setString( index++, dto.getNombreProyecto() );
			stmt.setString( index++, dto.getEstadoGrupo() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GrupoProyectoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the grupo_proyecto table.
	 */
	public void update(GrupoProyectoPk pk, GrupoProyecto dto) throws GrupoProyectoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getNumeroFicha() );
			stmt.setInt( index++, dto.getCodigo() );
			stmt.setString( index++, dto.getNombreProyecto() );
			stmt.setString( index++, dto.getEstadoGrupo() );
			stmt.setString( 5, pk.getNumeroFicha() );
			stmt.setInt( 6, pk.getCodigo() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GrupoProyectoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the grupo_proyecto table.
	 */
	public void delete(GrupoProyectoPk pk) throws GrupoProyectoDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getNumeroFicha() );
			stmt.setInt( 2, pk.getCodigo() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GrupoProyectoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the grupo_proyecto table that matches the specified primary-key value.
	 */
	public GrupoProyecto findByPrimaryKey(GrupoProyectoPk pk) throws GrupoProyectoDaoException
	{
		return findByPrimaryKey( pk.getNumeroFicha(), pk.getCodigo() );
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the criteria 'numero_ficha = :numeroFicha AND codigo = :codigo'.
	 */
	public GrupoProyecto findByPrimaryKey(String numeroFicha, int codigo) throws GrupoProyectoDaoException
	{
		GrupoProyecto ret[] = findByDynamicSelect( SQL_SELECT + " WHERE numero_ficha = ? AND codigo = ?", new Object[] { numeroFicha,  new Integer(codigo) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the criteria ''.
	 */
	public GrupoProyecto[] findAll() throws GrupoProyectoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY numero_ficha, codigo", null );
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the criteria 'numero_ficha = :numeroFicha'.
	 */
	public GrupoProyecto[] findByFicha(String numeroFicha) throws GrupoProyectoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE numero_ficha = ?", new Object[] { numeroFicha } );
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the criteria 'numero_ficha = :numeroFicha'.
	 */
	public GrupoProyecto[] findWhereNumeroFichaEquals(String numeroFicha) throws GrupoProyectoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE numero_ficha = ? ORDER BY numero_ficha", new Object[] { numeroFicha } );
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the criteria 'codigo = :codigo'.
	 */
	public GrupoProyecto[] findWhereCodigoEquals(int codigo) throws GrupoProyectoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE codigo = ? ORDER BY codigo", new Object[] {  new Integer(codigo) } );
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the criteria 'nombre_proyecto = :nombreProyecto'.
	 */
	public GrupoProyecto[] findWhereNombreProyectoEquals(String nombreProyecto) throws GrupoProyectoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE nombre_proyecto = ? ORDER BY nombre_proyecto", new Object[] { nombreProyecto } );
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the criteria 'estado_grupo = :estadoGrupo'.
	 */
	public GrupoProyecto[] findWhereEstadoGrupoEquals(String estadoGrupo) throws GrupoProyectoDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE estado_grupo = ? ORDER BY estado_grupo", new Object[] { estadoGrupo } );
	}

	/**
	 * Method 'GrupoProyectoDaoImpl'
	 * 
	 */
	public GrupoProyectoDaoImpl()
	{
	}

	/**
	 * Method 'GrupoProyectoDaoImpl'
	 * 
	 * @param userConn
	 */
	public GrupoProyectoDaoImpl(final Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "observador_de_proyectos.grupo_proyecto";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected GrupoProyecto fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			GrupoProyecto dto = new GrupoProyecto();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected GrupoProyecto[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			GrupoProyecto dto = new GrupoProyecto();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		GrupoProyecto ret[] = new GrupoProyecto[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(GrupoProyecto dto, ResultSet rs) throws SQLException
	{
		dto.setNumeroFicha( rs.getString( COLUMN_NUMERO_FICHA ) );
		dto.setCodigo( rs.getInt( COLUMN_CODIGO ) );
		dto.setNombreProyecto( rs.getString( COLUMN_NOMBRE_PROYECTO ) );
		dto.setEstadoGrupo( rs.getString( COLUMN_ESTADO_GRUPO ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(GrupoProyecto dto)
	{
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the specified arbitrary SQL statement
	 */
	public GrupoProyecto[] findByDynamicSelect(String sql, Object[] sqlParams) throws GrupoProyectoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GrupoProyectoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the grupo_proyecto table that match the specified arbitrary SQL statement
	 */
	public GrupoProyecto[] findByDynamicWhere(String sql, Object[] sqlParams) throws GrupoProyectoDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new GrupoProyectoDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
