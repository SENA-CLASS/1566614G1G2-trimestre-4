/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package co.edu.sena.controller.dao.jdbc;

import co.edu.sena.controller.dao.*;
import co.edu.sena.controller.factory.*;
import co.edu.sena.model.dao.dto.*;
import co.edu.sena.controller.dao.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class InstructorHasTrimestreDaoImpl extends AbstractDAO implements InstructorHasTrimestreDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT tipo_documento, numero_documento, ficha, id_trimestre, jornada, tipo_instructor FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( tipo_documento, numero_documento, ficha, id_trimestre, jornada, tipo_instructor ) VALUES ( ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET tipo_documento = ?, numero_documento = ?, ficha = ?, id_trimestre = ?, jornada = ?, tipo_instructor = ? WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ? AND id_trimestre = ? AND jornada = ? AND tipo_instructor = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ? AND id_trimestre = ? AND jornada = ? AND tipo_instructor = ?";

	/** 
	 * Index of column tipo_documento
	 */
	protected static final int COLUMN_TIPO_DOCUMENTO = 1;

	/** 
	 * Index of column numero_documento
	 */
	protected static final int COLUMN_NUMERO_DOCUMENTO = 2;

	/** 
	 * Index of column ficha
	 */
	protected static final int COLUMN_FICHA = 3;

	/** 
	 * Index of column id_trimestre
	 */
	protected static final int COLUMN_ID_TRIMESTRE = 4;

	/** 
	 * Index of column jornada
	 */
	protected static final int COLUMN_JORNADA = 5;

	/** 
	 * Index of column tipo_instructor
	 */
	protected static final int COLUMN_TIPO_INSTRUCTOR = 6;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 6;

	/** 
	 * Index of primary-key column tipo_documento
	 */
	protected static final int PK_COLUMN_TIPO_DOCUMENTO = 1;

	/** 
	 * Index of primary-key column numero_documento
	 */
	protected static final int PK_COLUMN_NUMERO_DOCUMENTO = 2;

	/** 
	 * Index of primary-key column ficha
	 */
	protected static final int PK_COLUMN_FICHA = 3;

	/** 
	 * Index of primary-key column id_trimestre
	 */
	protected static final int PK_COLUMN_ID_TRIMESTRE = 4;

	/** 
	 * Index of primary-key column jornada
	 */
	protected static final int PK_COLUMN_JORNADA = 5;

	/** 
	 * Index of primary-key column tipo_instructor
	 */
	protected static final int PK_COLUMN_TIPO_INSTRUCTOR = 6;

	/** 
	 * Inserts a new row in the instructor_has_trimestre table.
	 */
	public InstructorHasTrimestrePk insert(InstructorHasTrimestre dto) throws InstructorHasTrimestreDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setString( index++, dto.getTipoDocumento() );
			stmt.setString( index++, dto.getNumeroDocumento() );
			stmt.setString( index++, dto.getFicha() );
			stmt.setString( index++, dto.getIdTrimestre() );
			stmt.setString( index++, dto.getJornada() );
			stmt.setString( index++, dto.getTipoInstructor() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorHasTrimestreDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the instructor_has_trimestre table.
	 */
	public void update(InstructorHasTrimestrePk pk, InstructorHasTrimestre dto) throws InstructorHasTrimestreDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setString( index++, dto.getTipoDocumento() );
			stmt.setString( index++, dto.getNumeroDocumento() );
			stmt.setString( index++, dto.getFicha() );
			stmt.setString( index++, dto.getIdTrimestre() );
			stmt.setString( index++, dto.getJornada() );
			stmt.setString( index++, dto.getTipoInstructor() );
			stmt.setString( 7, pk.getTipoDocumento() );
			stmt.setString( 8, pk.getNumeroDocumento() );
			stmt.setString( 9, pk.getFicha() );
			stmt.setString( 10, pk.getIdTrimestre() );
			stmt.setString( 11, pk.getJornada() );
			stmt.setString( 12, pk.getTipoInstructor() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorHasTrimestreDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the instructor_has_trimestre table.
	 */
	public void delete(InstructorHasTrimestrePk pk) throws InstructorHasTrimestreDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getTipoDocumento() );
			stmt.setString( 2, pk.getNumeroDocumento() );
			stmt.setString( 3, pk.getFicha() );
			stmt.setString( 4, pk.getIdTrimestre() );
			stmt.setString( 5, pk.getJornada() );
			stmt.setString( 6, pk.getTipoInstructor() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorHasTrimestreDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the instructor_has_trimestre table that matches the specified primary-key value.
	 */
	public InstructorHasTrimestre findByPrimaryKey(InstructorHasTrimestrePk pk) throws InstructorHasTrimestreDaoException
	{
		return findByPrimaryKey( pk.getTipoDocumento(), pk.getNumeroDocumento(), pk.getFicha(), pk.getIdTrimestre(), pk.getJornada(), pk.getTipoInstructor() );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'tipo_documento = :tipoDocumento AND numero_documento = :numeroDocumento AND ficha = :ficha AND id_trimestre = :idTrimestre AND jornada = :jornada AND tipo_instructor = :tipoInstructor'.
	 */
	public InstructorHasTrimestre findByPrimaryKey(String tipoDocumento, String numeroDocumento, String ficha, String idTrimestre, String jornada, String tipoInstructor) throws InstructorHasTrimestreDaoException
	{
		InstructorHasTrimestre ret[] = findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ? AND id_trimestre = ? AND jornada = ? AND tipo_instructor = ?", new Object[] { tipoDocumento, numeroDocumento, ficha, idTrimestre, jornada, tipoInstructor } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria ''.
	 */
	public InstructorHasTrimestre[] findAll() throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY tipo_documento, numero_documento, ficha, id_trimestre, jornada, tipo_instructor", null );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'tipo_documento = :tipoDocumento AND numero_documento = :numeroDocumento AND ficha = :ficha'.
	 */
	public InstructorHasTrimestre[] findByInstructorFicha(String tipoDocumento, String numeroDocumento, String ficha) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? AND numero_documento = ? AND ficha = ?", new Object[] { tipoDocumento, numeroDocumento, ficha } );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'tipo_instructor = :tipoInstructor'.
	 */
	public InstructorHasTrimestre[] findByTipoInstructor(String tipoInstructor) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE tipo_instructor = ?", new Object[] { tipoInstructor } );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'id_trimestre = :idTrimestre AND jornada = :jornada'.
	 */
	public InstructorHasTrimestre[] findByTrimestre(String idTrimestre, String jornada) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_trimestre = ? AND jornada = ?", new Object[] { idTrimestre, jornada } );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'tipo_documento = :tipoDocumento'.
	 */
	public InstructorHasTrimestre[] findWhereTipoDocumentoEquals(String tipoDocumento) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE tipo_documento = ? ORDER BY tipo_documento", new Object[] { tipoDocumento } );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'numero_documento = :numeroDocumento'.
	 */
	public InstructorHasTrimestre[] findWhereNumeroDocumentoEquals(String numeroDocumento) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE numero_documento = ? ORDER BY numero_documento", new Object[] { numeroDocumento } );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'ficha = :ficha'.
	 */
	public InstructorHasTrimestre[] findWhereFichaEquals(String ficha) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ficha = ? ORDER BY ficha", new Object[] { ficha } );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'id_trimestre = :idTrimestre'.
	 */
	public InstructorHasTrimestre[] findWhereIdTrimestreEquals(String idTrimestre) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE id_trimestre = ? ORDER BY id_trimestre", new Object[] { idTrimestre } );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'jornada = :jornada'.
	 */
	public InstructorHasTrimestre[] findWhereJornadaEquals(String jornada) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE jornada = ? ORDER BY jornada", new Object[] { jornada } );
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the criteria 'tipo_instructor = :tipoInstructor'.
	 */
	public InstructorHasTrimestre[] findWhereTipoInstructorEquals(String tipoInstructor) throws InstructorHasTrimestreDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE tipo_instructor = ? ORDER BY tipo_instructor", new Object[] { tipoInstructor } );
	}

	/**
	 * Method 'InstructorHasTrimestreDaoImpl'
	 * 
	 */
	public InstructorHasTrimestreDaoImpl()
	{
	}

	/**
	 * Method 'InstructorHasTrimestreDaoImpl'
	 * 
	 * @param userConn
	 */
	public InstructorHasTrimestreDaoImpl(final Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "observador_de_proyectos.instructor_has_trimestre";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected InstructorHasTrimestre fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			InstructorHasTrimestre dto = new InstructorHasTrimestre();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected InstructorHasTrimestre[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			InstructorHasTrimestre dto = new InstructorHasTrimestre();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		InstructorHasTrimestre ret[] = new InstructorHasTrimestre[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(InstructorHasTrimestre dto, ResultSet rs) throws SQLException
	{
		dto.setTipoDocumento( rs.getString( COLUMN_TIPO_DOCUMENTO ) );
		dto.setNumeroDocumento( rs.getString( COLUMN_NUMERO_DOCUMENTO ) );
		dto.setFicha( rs.getString( COLUMN_FICHA ) );
		dto.setIdTrimestre( rs.getString( COLUMN_ID_TRIMESTRE ) );
		dto.setJornada( rs.getString( COLUMN_JORNADA ) );
		dto.setTipoInstructor( rs.getString( COLUMN_TIPO_INSTRUCTOR ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(InstructorHasTrimestre dto)
	{
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the specified arbitrary SQL statement
	 */
	public InstructorHasTrimestre[] findByDynamicSelect(String sql, Object[] sqlParams) throws InstructorHasTrimestreDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorHasTrimestreDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the instructor_has_trimestre table that match the specified arbitrary SQL statement
	 */
	public InstructorHasTrimestre[] findByDynamicWhere(String sql, Object[] sqlParams) throws InstructorHasTrimestreDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new InstructorHasTrimestreDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
